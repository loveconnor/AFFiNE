# LoveNotes Hooks

> This directory will be moved to `@lovenotes/worksapce/lovenotes/hooks` in the future.

Only put hooks in this directory if they are specific to LoveNotes, for example
if they are using the LoveNotes API, or if the `LoveNotesWorkspace` is required.
